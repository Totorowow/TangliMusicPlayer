<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tangli.musicplayer">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.SplashActivity"
            android:theme="@style/SplashTheme"
            android:noHistory="true"
            android:configChanges="orientation|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.tangli.musicplayer.activities.MainActivity"
            android:theme="@style/AppTheme.Main"
            >
        </activity>
        <activity
            android:name="com.tangli.musicplayer.activities.DetailActivity"
            android:theme="@style/AppTheme.Detail">
        </activity>

        <service
            android:name="com.tangli.musicplayer.music.PlayerService"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>
